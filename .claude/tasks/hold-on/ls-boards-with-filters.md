# List board

`klaro list boards` should show the filters set on a board

## Technically

* `filters` field, to be friendly shown in the table.

## Status: HOLD

API does not currently return the `filters` field for boards.

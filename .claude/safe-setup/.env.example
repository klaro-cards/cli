# Git configuration (used for commits inside the container)
GIT_USER_NAME=Your Name
GIT_USER_EMAIL=your.email@example.com
COMPOSE_PROJECT_NAME=your-project

# Actual configuration for acceptance tests (use with care!!)
# KCLI_USER=
# KCLI_PASSWORD=
# KCLI_SUBDOMAIN=
# KCLI_BOARD=
